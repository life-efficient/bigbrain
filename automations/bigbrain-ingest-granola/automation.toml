version = 1
id = "bigbrain-ingest-granola"
kind = "cron"
name = "BigBrain Ingest Granola"
prompt = "Use the BigBrain: Granola Ingest skill. Ingest recent Granola meetings into the selected BigBrain brain. Resolve the brain home through BigBrain's normal selection order, read the live filing rules before writing, use folder-aware Granola MCP tools when folder exclusions are required, skip exact-title New note / New Note records, preserve verbatim transcript sidecars after safety review, and update grounded meeting/entity/task pages. Handle entity updates as part of the run: review relevant people, company/organization, deal, concept, project, and other supported pages, then update them when meeting evidence adds durable facts, relationship context, decisions, status changes, or commitments; at minimum, append evidence-backed timeline entries that cite the Granola meeting ID and date. Handle tasks as part of the run: check existing open/in-progress/waiting task pages before creating new ones, update matching tasks when meeting evidence changes status, owner, due date, next action, or completion criteria, and create new task pages only for concrete assignable follow-ups. Run `bigbrain sync --json`, verify changed tasks are visible through the task surface when available, and report the checked window, excluded folders, ingested/repaired/skipped meetings, changed entity pages, changed tasks, sidecars, transcript safety result, sync result, and blockers. Do not ingest from user-named excluded Granola folders if folder filtering is unavailable."
status = "ACTIVE"
rrule = "FREQ=DAILY;BYHOUR=2;BYMINUTE=30;BYSECOND=0"
model = "gpt-5.5"
reasoning_effort = "low"
execution_environment = "local"
cwds = ["<brain-home>"]
created_at = 1782690600000
updated_at = 1782690600000
