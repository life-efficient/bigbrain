interface:
  display_name: "BigBrain: Setup"
  short_description: "Initialize BigBrain, verify the runtime, and migrate existing notes when needed"
  default_prompt: "Use BigBrain: Setup to initialize a brain home, verify health and sync, optionally migrate existing markdown, and prove retrieval works."
