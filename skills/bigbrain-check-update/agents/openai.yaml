interface:
  display_name: "BigBrain: Check Update"
  short_description: "Safely update and verify a source-managed BigBrain install"
  default_prompt: "Use $bigbrain-check-update as the source-install fallback, or route a packaged desktop install to its in-app updater."
