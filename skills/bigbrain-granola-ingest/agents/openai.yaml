interface:
  display_name: "BigBrain Ingest Granola Meetings"
  short_description: "Ingest recent Granola meetings into BigBrain"
  default_prompt: "Use $bigbrain-granola-ingest to import recent Granola meetings into BigBrain, route machine-wide candidates by brain description only, respect excluded folders, preserve transcript sidecars, update relevant entity and task pages, sync the index, and report with a count-first summary followed by headings for each brain that had meetings ingested and one privacy-safe bullet per meeting."
