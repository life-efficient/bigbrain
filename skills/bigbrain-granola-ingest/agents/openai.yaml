interface:
  display_name: "BigBrain Ingest Granola Meetings"
  short_description: "Ingest recent Granola meetings into BigBrain"
  default_prompt: "Use $bigbrain-granola-ingest to import recent Granola meetings into BigBrain, check the optional Personal Brain meeting-ingestion protocol before destination writes, route machine-wide candidates by brain description only with Personal Brain as the fallback for otherwise unclear processable meetings, respect excluded folders, preserve transcript sidecars, update relevant entity and task pages, sync the index, and report with a count-first summary followed by headings for each brain that had meetings ingested and one meeting-title bullet per meeting plus a needs-attention heading for blockers."
