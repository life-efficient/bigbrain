interface:
  display_name: "BigBrain Ingest Granola Meetings"
  short_description: "Ingest recent Granola meetings into BigBrain"
  default_prompt: "Use $bigbrain-granola-ingest to discover and route recent Granola meetings, claim each route, record the post-delegation continuation checklist, and invoke $bigbrain-meeting-ingest in delegated mode for exactly one meeting at a time. After every delegated result, explicitly regain control and complete Granola destination sync, final read-back, ledger verify or fail, cursor advancement only after verification, remaining batch work, and the count-first privacy-safe report."
