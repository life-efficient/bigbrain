interface:
  display_name: "BigBrain: Maintain"
  short_description: "Check BigBrain, attempt safe fixes, and report what remains"
  default_prompt: "Use BigBrain: Maintain to check BigBrain, attempt safe bounded fixes for health findings, re-run health, and report anything that still needs manual follow-up."
